version: '3.8'

services:
  expo-app:
    image: node:20
    working_dir: /app
    ports:
      - "8081:8081"
      - "19000:19000"
      - "19001:19001"
    volumes:
      - ./app:/app
      - /app/node_modules
    command: bash -c "npm install && npm start"
